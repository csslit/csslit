import { css } from "@csslit/core";

export const accent = "rebeccapurple";

export const globalStyles = css.global`
  body {
    margin: 0;
  }
`;

export const panel = css`
  display: grid;
  min-height: 100vh;
  place-items: center;
  color: white;
  background: ${accent};
  font: 600 2rem/1.2 system-ui;
`;

<main className={panel}>csslit + MDX</main>
