{
  "name": "csslit-monorepo",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "packages/*",
    "playground",
    "tests"
  ],
  "type": "module",
  "scripts": {
    "build": "vp run -r build",
    "test": "vp run -r test"
  },
  "devDependencies": {
    "vite-plus": "^0.2.0"
  },
  "engines": {
    "node": ">=24.11.0"
  },
  "packageManager": "npm@11.17.0"
}
