{
  "name": "csslit-monorepo",
  "private": true,
  "engines": {
    "node": ">= 20"
  },
  "workspaces": [
    "packages/*",
    "playground"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "test": "npm run test --workspaces"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.5.0",
    "vite": "^8.0.0"
  }
}
